<p id="notice"><%= notice %></p>

<h1>Admin Pens</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @pens.each do |pen| %>
      <tr>
        <td><%= link_to 'Show', admin_pen_path(pen) %></td>
        <td><%= link_to 'Edit', edit_admin_pen_path(pen) %></td>
        <td><%= link_to 'Destroy', pen, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Admin Pen', new_admin_pen_path %>
